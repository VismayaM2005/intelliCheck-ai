version: '3.8'

services:
  intellicheck-ai:
    build: .
    container_name: intellicheck-ai
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - APP_ENV=production
      - MODEL_VERSION=v3
    restart: unless-stopped
